# Nest backend
PORT=9090
TURSOR_AI_URL=http://127.0.0.1:8000

# CDP (Playwright / Chromium)
CDP_HEADLESS=false
CDP_STEP_DELAY_MS=1000
# CDP_SLOW_MO=100

# Supabase + AI credentials: workspace only — NOT here.
#   {workspace}/.tursor/config.json → supabase.bucket, supabase.database, ai
# See Tursor-Backend/docs/SUPABASE_SETUP.md
