# Nest backend
PORT=9090
TURSOR_AI_URL=http://127.0.0.1:8000

# CDP (Playwright / Chromium)
CDP_HEADLESS=false
CDP_STEP_DELAY_MS=1000
# CDP_SLOW_MO=100

# Supabase Storage — configure per workspace in .tursor/config.json:
#   "supabase": { "url", "serviceRoleKey", "storageBucket" }
